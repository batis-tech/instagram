<nav class="navbar navbar-expand-lg navbar-white bg-white">
    <div class="container">
      <div class="navbar-brand">
         <i class="fa fa-instagram"></i> | <%= link_to 'LikeInsta',account_signed_in? ? dashboard_path : root_path, class: "navbar-brand" %>
      </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent" >
            <ul class="navbar-nav ml-auto">
              <% if account_signed_in? %>
              <%= link_to 'Home',dashboard_path, class: "nav-link" %>
              <%= link_to 'New Post',new_post_path, class: "nav-link" %>
              <%= link_to 'My Profile', profile_path(current_account.username), class: "nav-link" %>
              <%= link_to 'Sign out',destroy_account_session_path, method: :delete, class: "btn folat-right" %>
              <% else %>
              <%= link_to "Sign-in",  new_account_session_path,class: "btn btn-outline-success folat-right" %>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
